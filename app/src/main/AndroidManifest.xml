<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ismatkhanam.on_button_click">

    <uses-permission android:name="android.permission.SET_WALLPAPER"></uses-permission>
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="com.example.ismatkhanam.on_button_click.MAINACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="com.example.ismatkhanam.on_button_click.MENU" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".SQLView">
            <intent-filter>
                <action android:name="com.example.ismatkhanam.on_button_click.SQLVIEW" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".TextPlay"
            android:theme="@android:style/Theme.Translucent"></activity>

        <activity android:name=".Email"></activity>

        <activity android:name=".Camera"
            android:label="Camera Application"
            android:screenOrientation="portrait">
        </activity>

        <activity android:name=".Data"></activity>

        <activity android:name=".OpenedClass"></activity>

        <activity android:name=".AboutUs" android:theme="@android:style/Theme.Dialog" >
            <intent-filter>
                <action android:name="com.example.ismatkhanam.on_button_click.ABOUT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".Prefs" android:theme="@android:style/Theme.Dialog" >
            <intent-filter>
                <action android:name="com.example.ismatkhanam.on_button_click.PREFS"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".GFX"></activity>

        <activity android:name=".GFXSurface"></activity>

        <activity android:name=".SoundStuff"></activity>

        <activity android:name=".Slider"></activity>

        <activity android:name=".Tabs"></activity>

        <activity android:name=".SimpleBrowser"></activity>

        <activity android:name=".Flipper"></activity>

        <activity android:name=".SharedPrefs"></activity>

        <activity android:name=".InternalData"></activity>

        <activity android:name=".ExternalData"></activity>

        <activity android:name=".SQLiteExample"></activity>

        <activity android:name=".Accelerate"></activity>

        <activity android:name=".HttpExample"></activity>

        <activity android:name=".WeatherXMLParsing"></activity>
        
        <receiver android:name=".PointlessWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_stuff">
            </meta-data>
        </receiver>

        <activity android:name=".WidgetConfig">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE"/>
            </intent-filter>
        </activity>

        <activity android:name=".GLExample"></activity>

        <activity android:name=".GLCubeEx"></activity>

        <activity android:name=".Voice"></activity>

        <activity android:name=".TextVoice"></activity>

        <activity android:name=".StatusBar"></activity>

        <activity android:name=".SeekBarVolume"></activity>

    </application>

</manifest>
